import { createStyles, rem } from '@mantine/core'

export default createStyles((theme) => ({
  detailProduct: {
    margin: `${rem(50)} 0`,
  },
  relatedProduct: {
    margin: `${theme.spacing.xl} 0`,
  },
}))
